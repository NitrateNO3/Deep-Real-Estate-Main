{
  "mcpServers": {
    "vercel": {
      "command": "node",
      "args": ["tools/vercel-mcp/server.mjs"],
      "env": {
        "VERCEL_TOKEN": "${VERCEL_TOKEN}",
        "VERCEL_TEAM_ID": "${VERCEL_TEAM_ID:-}"
      }
    }
  }
}
